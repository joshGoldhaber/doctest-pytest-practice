[flake8]
# Doctest lines must stay on one line each, and several of the provided
# doctests are longer than the 79-character default. 140 clears them all.
max-line-length = 140
exclude = .git,__pycache__,.pytest_cache
